namespace Feliz.QRCode

open Fable.Core
/// This interface allows us to stop adding random props to the qrcode.react.
type IQRCodeProp =
    interface
    end

type IQRCodeCanvasProp =
    interface
    end

type IQRCodeSVGProp =
    interface
    end

type IImageSettingsProp =
    interface
    end
